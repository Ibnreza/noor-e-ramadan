name: ramadan_app
description: "Noor Ramadan - The #1 Islamic App for 2026."
publish_to: 'none'
version: 2.0.0+2026

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: '>=3.27.0' 
  
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  
  adhan_dart: ^1.2.0
  # Downgraded flutter_qiblah to 3.1.0+1 to work with geolocator 13.x
  # flutter_qiblah: ^3.1.0+1   # disabled for web build (Qibla works on Android/iOS)
  
  fl_chart: ^0.70.0
  geolocator: ^13.0.4
  flutter_local_notifications: ^18.0.1
  vibration: ^3.1.2
  confetti: ^0.8.0
  
  intl: any 
  timezone: ^0.9.0
  
  google_fonts: 6.2.1
  flutter_animate: ^4.5.2
  sensors_plus: ^6.1.1
  permission_handler: ^11.3.1
  path_provider: ^2.1.5
  share_plus: ^10.1.3
  screenshot: ^3.0.0
  device_info_plus: ^11.2.0
  package_info_plus: ^8.1.2
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  hive_generator: ^2.0.1
  riverpod_generator: ^2.4.0
